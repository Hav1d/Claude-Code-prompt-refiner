{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "python -X utf8 -m src.hook_entry UserPromptSubmit --config prompt-config.json",
            "timeout": 45000
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(python -X utf8 -m src.*)",
      "Bash(pip install*)"
    ]
  },
  "_comment": "Local development config. For plugin mode, hooks/hooks.json is used instead."
}
